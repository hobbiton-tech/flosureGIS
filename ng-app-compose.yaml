version: '2'
services:
  go-web-app:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      - VIRTUAL_HOST=savenda-flosure.com
      - LETSENCRYPT_HOST=savenda-flosure.com