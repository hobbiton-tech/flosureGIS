export interface IVehicleType {
    id: string;
    vehicleType: string;
    description: string;
}
export interface IVehicleMake {
    id: string;
    vehicleMake: string;
    description: string;
}

export interface IVehicleModel {
    id: string;
    vehicleModel: string;
    description: string;
}
